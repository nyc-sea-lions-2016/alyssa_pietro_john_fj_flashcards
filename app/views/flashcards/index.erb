<main>
  <ul>
    <% @decks.each do |deck| %>
      <li>
        <form method="POST" action="/games">
          <input type="submit" name="game_info[deck_id]" value="<%= deck.name %>"><br>
        </form>
      </li>
    <% end %>
  </ul>
</main>