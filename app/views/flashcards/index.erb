<main>
  <ul>
    <% @deck.each do |deck| %>
      <li>
        <form method="POST" action="/games">
          <input type="submit" value="<% @game.id %>"><%= deck.name %><br>
        </form>
      </li>
    <% end %>
  </ul>
</main>